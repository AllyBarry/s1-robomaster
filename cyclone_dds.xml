<?xml version="1.0" encoding="UTF-8" ?>
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config
            https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
    <Domain id="any">
        <SharedMemory>
            <Enable>false</Enable>
        </SharedMemory>
        <General>
            <!-- Allow multicast for discovery only (SPDP), keep data transfer unicast -->
            <AllowMulticast>spdp</AllowMulticast>
        </General>
        <Discovery>
            <!-- Using SPDP multicast for discovery; peers found automatically.
                 Add <Peers><Peer address="x.x.x.x"/></Peers> here only if
                 devices are on a different subnet where multicast doesn't reach. -->
            <ParticipantIndex>auto</ParticipantIndex>
        </Discovery>
    </Domain>
</CycloneDDS>
